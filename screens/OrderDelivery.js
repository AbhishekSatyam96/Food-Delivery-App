import React from 'react';
import {Text, View} from 'react-native'

const OrderDelivery = () => {

    const renderMap = () => {
        
    }

    return(
        <View style={{flex: 1}}>
            {renderMap()}
        </View>
    )
}

export default OrderDelivery;